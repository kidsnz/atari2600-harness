# Technique — sprite placement physics (where a strobe lands an object, and when a write is late)

**Goal:** know, before designing a kernel, exactly where a strobe puts an object, how late a
graphics write can be and still reach the copy it feeds, and which distances need HMOVE — because
that is what decides whether a shape wider than eight pixels can be drawn at all.

**Status:** ✅ measured and CI-locked. Fixture `roms/litmus/litmus_sprite_place.asm` (generated by
`scripts/gen_litmus_sprite_place.py`, eleven bands of four scanlines); grading
`internal/emu/spriteplace_test.go`.
**Source:** measured here 2026-08-21 while fitting a 12 px letterform onto the 2600 for a piece in
the private `roms/` repository — its own notes carry the full trail and name this file. The rest of
this catalogue says what to DO with sprites
(`nusiz-shaping.md`, `dynamic-multisprite.md`, `bitmap48.md`); none of it said where they land, so
the numbers were re-derived from throwaway probes twice in one session. This file is the fix.

## The rules

| | rule | band |
|---|---|---|
| 1 | a `RESP` strobe whose **write cycle** is c puts a NORMAL-width player at **x = 3c − 60** | 0, 1 |
| 2 | a `RESM` strobe puts a missile at **x = 3c − 61** — one clock further LEFT from the same cycle | 2, 3 |
| 3 | so `RESM` three cycles after `RESP` puts the missile at **exactly player + 8** | 4 |
| 4 | a missile follows its player's NUSIZ copies | 5 |
| 5 | a strobe **does not draw the new position on the line it runs on** | 6 |
| 6 | a GRP write takes effect at **screen x = 3w − 64** — four colour clocks of margin, no more | 7 |
| 7 | a normal-width player **cannot be strobed left of x = 3** | 8, 9 |
| 8 | a strobe cancels the pending draw of the **FIRST copy only** | 10 |
| 9 | a DOUBLE-width or QUAD-width player lands at **x = 3c − 59**, never left of x = 4 | — |

Rule 9 is from the same session's probes rather than from this litmus, which grades normal width.
Width itself is free: double and quad are the same single NUSIZ write and the same landing place;
what they cost is screen area.

## What rule 3 buys

Two strobes on one line are at least three cycles apart, because that is the shortest store, and
three cycles is nine colour clocks. **Two players can therefore never sit 8 px apart** — the gap is
nine or more. Nor can the glyph be shifted inside its byte to close it: split a 12 px shape into a
w-bit head and a (12−w)-bit tail and the head can move at most 8−w px inside its byte, so the
reachable gap tops out at w + (8 − w) = 8, one short, whatever the split.

The usual escape is HMOVE, and HMOVE **blanks the leftmost 8 px of the line it is strobed on** —
fine for a 48 px band parked at x=87 (see `bitmap48.md`), fatal for a picture that starts at x=3.

Rule 2 removes the need. A missile strobed three cycles after its player lands at
3(c+3) − 61 = (3c − 60) + 8, exactly where an 8 px player stops drawing:

```
        sta RESP0       ; write cycle c    -> player head at x
        sta RESM0       ; write cycle c+3  -> missile tail at x+8, abutting
```

With rule 4 on top, `NUSIZ0 = $26` (three copies 32 apart, missile 4 px wide) draws **three
12-pixel shapes on one scanline out of one player and one missile**, positioned by two strobes and
no HMOVE at all. The missile is solid, so the 4 px tail is all-on or all-off: a shape whose last
four pixels are not uniform cannot use it.

## Rule 6, and why the number matters

Band 7 parks P0 at x=30 with copies at 30, 62 and 94, puts GRP0 up as $FF in HBLANK and drops it
to $00 partway across. Copy 0 has already drawn; copy 1 is the question.

| write cycle | lands at x | effective at x | copy 1 at 62 |
|---|---|---|---|
| 43 | 61 | 65 | **keeps three pixels of the old byte** (65 − 62) |
| 42 | 58 | 62 | clean |
| 41 | 55 | 59 | clean |

So **four clocks of margin is enough and one is not**. Six — which the kernels that prompted this
assumed for a while, after a three-clock write failed — is two too many, and those two clocks were
the whole distance between eight letters on a row and ten.

## Rule 8, and a thing that looks free and is not

A strobe cancels a pending draw, so it is tempting to skip a blank line's clear when the strobe
gets there first. Band 10 says what actually happens: P0 parked at 42 with copies at 42, 74 and 106
is strobed to x=24 while the beam is at 16, ahead of all three, and the strobe line comes out with
**56 and 88 but not 24**. Copy 0 is cancelled; the copies NUSIZ makes at +32 and +64 are triggered
from the reset position and land anyway. **A multi-copy object must be cleared.**

## Don't strobe HMOVE mid-line to hide the bar

Measured, and it does not work: with HMP0 = left 3, a mid-line HMOVE at write cycle 35 and at 50
moved the object **not at all**, at 62 moved it correctly (from the next line), and at 72 moved it
**eleven** pixels and then the object vanished. `internal/emu/hmovemid_test.go` already says the
same thing from the other direction — of the three strobe positions `litmus_hmove_mid` tries, one
shifts and two do nothing.

## What this made possible, and where the tool for it lives

Rules 2, 3, 4, 6 and 8 together put **all ten letters of a 156 px word on ONE scanline row** at
double width, five letters a scanline, four scanlines per row of the artwork.

The scheduler that emits those lines lives with that work, in the private `roms/` repository, and
is deliberately still there rather than in `pkg/`: this catalogue's own pipeline promotes a
generator only when it is *stable and reusable*, and that one changed shape five times in the
session that produced it. (Anyone working from the umbrella has both repositories; the work's own
notes give the path.) What it does is general, and is what a beam-window scheduler promoted into
`pkg/` would have to offer:

> given a list of TIA register writes, each with the earliest and latest **beam position** its
> store may occupy, plus which registers are free to hold a value in flight, emit a cycle-exact
> instruction sequence — or name the constraint that binds.

Three things it learned that any such tool needs: only the STORE has to sit inside the window, so
loads belong in the waits; earliest-deadline-first must be able to INSERT (a first copy has no
floor, is "ready" from cycle 0, and plain EDF runs it ahead of tighter work that is merely not
released yet); and a blank line's chores have no fixed order, since a clear only has to beat the
beam to where its object is parked. Nothing in this repository synthesises a kernel today —
`prove_line_budget`, `beam_intervals` and `cyclebound` all analyse one that already exists.

## Verified
Eleven bands: players at 12 and 60, missiles at 11 and 59, the abutting pair at 30 and 38, three
copies at 30/62/94 with their missiles at 38/70/102, the strobe line showing 30 and the line after
showing 60, the three-pixel leak at write cycle 43 and none at 42 or 41, the clamp at x=3 with
22 and 25 obeying 3c − 60 above it, and the mid-line strobe leaving 56 and 88 with 24 gone.
Both new assertions were negative-controlled: changing the expected leak from 3 to 2, and adding
the cancelled copy back to band 10, each make the test fail.
